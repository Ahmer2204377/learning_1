const stats={
    min : 40,
    max : 70,
    mid :55,
    
}

const sentence =`my electric bike min speed is ${stats.min} and max speed is ${stats.max} and average speed for
 higher efficiency is ${stats.mid}. `

console.log(sentence);